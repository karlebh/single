## Single is a Laravel VueJs single page application.

# It contains a Login and Registration system using JSON Web Tokens (JWT).

# It also implements a basic crud operation exclusive only to authenticated users.